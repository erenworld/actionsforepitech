name: EpiActions

run-name: ${{ github.actor }} is testing out EpiActions

on:
  push:
    branches:
    - main
    - '!ga-ignore-'
  pull_request:
    branches:
    - main
    - '!ga-ignore-'

env:
  MIRROR_URL: "mirror"
  EXECUTABLES: "mysh"

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
        - run: echo "🎉 WELCOME !"
        - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
        - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
        - name: Check out repository code
          uses: actions/checkout@v4
        - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
        - run: echo "🖥️ The workflow is now ready to test your code on the runner."
        - name: List files in the repository
          run: |
            ls ${{ github.workspace }}
        - run: echo "🍏 This job's status is ${{ job.status }}."
        - run: echo "end"
    
  check_coding_style:
    name: "Check codingstyle"
    runs-on: ubuntu-latest
    container: ghcr.io/epitech/coding-style-checker:latest
    steps:
        - name: Check out codingstyle
          uses: actions/checkout@v4
        - name: Run codingstyle
          id: check_coding_style
          run: |
            check $(pwd) $(pwd)
            cat $(pwd)/coding-style-reports.log

  check_program_compilation:
    runs-on: ubuntu-latest
    needs: [check_coding_style]
    steps:
        - name: Check out repository code
          uses: actions/checkout@v4
        - name: Make compilation
          run: make
          timeout-minutes: 2
        - name: Make clean compilation
          run: make clean
        - uses: pixta-dev/repository-mirroring-action@v1
          with:
            target_repo_url:
              git@github.com:erenworld/actionsforepitech.git
            ssh_private_key:                            
              ${{ secrets.GIT_SSH_PRIVATE_KEY }}
